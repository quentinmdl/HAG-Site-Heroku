<nav class="navbar navbar-expand-lg ">
	<div class="container">
		<a class="navbar-brand" href="{{ path('main') }}">
			<img src="{{ asset('build/images/AHAG.webp') }}" width="125" height="auto" class="d-inline-block align-top" alt="image du site">
		</a>
		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon">
          <i class="fa fa-bars" style="color:#fff; font-size:28px;"></i>
      </span>
		</button>

		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<div class="container">
				<ul class="navbar-nav mr-auto header-navbar">
					<li class="nav-item active">
						<a class="nav-link" href="{{ path('main') }}">Accueil
							<span class="sr-only">(current)</span>
						</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('articles') }}">Articles</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('play') }}">Jouer</a>
					</li>
					{% if not app.user %}


					{% else %}
						<li class="nav-item">
							<a class="nav-link" href="{{ path('app_user_session_challenges')}}">Ma session</a>
						</li> 
						<li class="nav-item">
							<a class="nav-link" href="{{ path('app_show_group')}}">Mon groupe</a>
						</li> 
					{% endif %}
					
				</ul>
				<div>
					<ul class="navbar-nav mr-auto header-navbar">
						{% if not app.user %}
							<li class="nav-item">
								<a class="nav-link" href="{{ path('app_login') }}">Connexion/Inscription</a>
							</li>
						{% else %}
							<li class="nav-item dropdown">
								<a class="nav-link dropdown-toggle" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
									{% if app.user.file is empty %}
			
									{% else %}
									<img src="{{ vich_uploader_asset(app.user, 'profileFile') }}"  width="50" heigth="auto" alt="image de profil">
									{% endif %}
									{{ app.user.username() }}
								</a>
								<div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
									<a class="dropdown-item" href="{{ path('app_user_profile')}}">Gestion du compte</a>
									<a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a>
								</div>
							</li>
						{% endif %}
						{% if is_granted('ROLE_ADMIN')%}
							<li class="nav-item">
								<a class="nav-link" href="{{ path('admin') }}">Administration</a>
							</li>
						{% endif %}
					</ul>
				</div>
			</div>
		</div>
	</div>
</nav>

